name: Metrics
on:
  # Schedule updates (each hour)
  schedule: [{cron: "0 * * * *"}]
  # Lines below let you run workflow manually and on each commit
  workflow_dispatch:
  push: {branches: ["master", "main"]}
jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: lowlighter/metrics@latest
        with:
          # Your GitHub token
          # The following scopes are required:
          #  - public_access (default scope)
          # The following additional scopes may be required:
          #  - read:org      (for organization related metrics)
          #  - read:user     (for user related metrics)
          #  - read:packages (for some packages related metrics)
          #  - repo          (optional, if you want to include private repositories)
          token: ${{ secrets.METRICS_TOKEN }}

          # Options
          user: Daniel-Deepak-Dev
          template: classic
          base: header, activity, community, repositories, metadata
          config_timezone: Asia/Kolkata

          # Plugin: Introduction (Title & detailed statistics)
          plugin_introduction: yes
          plugin_introduction_title: yes

          # Plugin: Isometric Calendar (3D commit calendar - visually stunning)
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year

          # Plugin: Languages (Detailed breakdown of languages used in commits)
          plugin_languages: yes
          plugin_languages_ignored: >-
            html, css, tex, less, dockerfile, makefile, qmake, lex, cmake, shell,
            gnuplot
          plugin_languages_limit: 8
          plugin_languages_sections: most-used
          plugin_languages_colors: github
          plugin_languages_threshold: 0%
          plugin_languages_details: bytes-size, percentage

          # Plugin: Habits (Coding facts based on commits)
          plugin_habits: yes
          plugin_habits_charts: yes
          plugin_habits_days: 14
          plugin_habits_facts: yes
          plugin_habits_from: 200

          # Plugin: Topics (Badges for topics/tech stack used in your repos)
          plugin_topics: yes
          plugin_topics_sort: stars
          plugin_topics_mode: icons
          plugin_topics_limit: 15

          # Plugin: Notable Contributions (Contributions to popular repos)
          plugin_notable: yes
          plugin_notable_from: organization

          # Plugin: Achievements (Trophies for GitHub Goals)
          plugin_achievements: yes
          plugin_achievements_display: detailed
          plugin_achievements_threshold: C

          # Plugin: Traffic (Views)
          plugin_traffic: yes
